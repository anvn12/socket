#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <sstream>

using namespace std;

vector<string> processCommandString(string command);

bool isValidIP(string ip);

bool isNum(string temp);

